# Роадмап: рабочее место по производительности 1С (техжурнал)

Цель: из консольного нормализатора (`cpp_parse`, ~3.7 ГБ/с) сделать полноценное рабочее место:
**агент-сборщик → приёмник → хранилище (ClickHouse первым, затем PG/DuckDB/MSSQL) → Grafana → свой UI**.

Решения владельца продукта (2026-07-03):
- СУБД: все четыре за слоем абстракции; первая — ClickHouse; + Grafana и Prometheus-метрики.
- Клиент: сначала Grafana, свой UI вторым этапом.
- Агент: онлайн-слежение (служба) + разовый импорт архива.
- Агентская часть: **bake-off Go vs Rust vs C++** по протоколу [docs/bakeoff-protocol.md](docs/bakeoff-protocol.md), победитель — по цифрам.

---

## Фаза 0 — фундамент (текущая)

- [x] Разобрать репозиторий, убрать старые попытки в `_Архив/`
- [x] Карта исходников нормализатора → [docs/normalizer-source-map.md](docs/normalizer-source-map.md)
- [x] Инвентаризация событий корпуса 175 ГБ → [docs/event-inventory.md](docs/event-inventory.md) (18 типов, ~193 млн событий)
- [x] Проект слоя хранения → [docs/storage-design.md](docs/storage-design.md)
- [x] Протокол bake-off и тест-стратегия → [docs/bakeoff-protocol.md](docs/bakeoff-protocol.md)
- [x] Спецификация формата v1.0 (draft) → [docs/format-spec.md](docs/format-spec.md)
- [x] `git init` + первый коммит (`c0c9caa`)
- [x] Тулчейны: VS Build Tools 2022 (MSVC 19.44), CMake 4.3, rustup, hyperfine; сборка ядра: `cl /O2 /GL /arch:AVX2 /DNOMINMAX`
- [x] **Починены критичные баги ядра**: KI-2 (строгая грамматика чисел + канонизация duration), KI-5 (дренаж очереди + exit-код при ошибке writer), KI-6 (пропуск входного BOM — вернул +1 событие на файл: 21307 против 21112 на CallsDiag_86), KI-7 (убран выходной BOM), KI-8 (валидация workers), KI-12 (счётчик ошибок mmap + exit 2)
- [x] Golden-тесты: 19 кейсов (12 синтетических + 7 реальных), 18 PASS + 1 XFAIL (KI-1), эталоны зафиксированы, все 1501 строка — валидный JSON; раннер `tests/golden/run_golden.ps1`

## Фаза 1 — быстрая польза: архив → ClickHouse → Grafana

- [x] ClickHouse поднят, схема применена и проверена вживую (все 4 MV работают). Native-порт хоста: **9001**
- [x] Импортёр `deploy/importer/import-jsonl.ps1`: NDJSON → `tj.events` целиком на SQL ClickHouse (JSONAsString + JSONExtract), проверен на сэмпле CallsDiag_86 (21112 строк = 21112 строк в БД, горячие колонки и props корректны). Найдено и починено: `CLICKHOUSE_SKIP_USER_SETUP` (доступ с хоста), TTL по умолчанию 3650 дней (30-дневный молча удалял импортированный архив)
- [x] Grafana provisioning: datasource ClickHouse + дашборд «Обзор кластера» (6 панелей) — проверить вживую после полной заливки
- [x] Полный архив залит: **121 485 342 события за 33 мин**, на диске 5.59 ГиБ (сжатие **27×** против несжатых 152 ГиБ — лучше оценки 12–18×). Grafana: datasource OK, дашборд отвечает реальными данными (стартовый период выставлен на архив 28.11–01.12.2025)
- [x] Память укрощена: `.wslconfig` (потолок WSL 10 ГБ, autoMemoryReclaim) + `mem_limit: 6g` для ClickHouse — виртуалка ужалась с 15.8 до ~5 ГБ
- [ ] Смоук сравнения периодов на данных 28–30.11.2025

## Фаза 2 — bake-off агентов (Go / Rust / C++)

- [x] **Go-участник готов** (`agents/go`): 18 PASS golden, байт-в-байт с C++ ядром на реальных коллекциях, детерминизм при любом workers, OOM-риск найден и закрыт верификацией. Спека дозрела до rev 3 по итогам независимой реализации
- [x] **Rust-участник готов** (`agents/rust`): golden с первого прогона, **0 пробелов спеки** (rev 3 самодостаточна), 30-кейсовый трёхсторонний дифф-фаззинг чист, верификация нашла и починила 1 баг (пути с `..`)
- [x] **Ядро вынесено в `core/`** (он же C++-участник): библиотека tj_core + CLI с обоими контрактами + C ABI (FFI с self-test). Скользящее окно mmap 64 МБ (на файле 5.76 ГБ: RSS 2.4 ГБ против 8 ГБ у эталона), упорядоченный вывод при любом threads, **~7× быстрее эталонного exe**. Верификация закрыла дыры exception-safety на границе FFI и отклонения флагов от протокола. Известная особенность сборки: генератор NMake ломается на кириллице пути — использовать «Visual Studio 17 2022» (см. core/README.md)
- [x] Предварительный замер (не зачётный): **C++ core 2231–2588 МБ/с > Rust 1845–2192 > Go 1663–1941**; RSS у Go ~490 МБ против ~170 у Rust/C++; эталон ~365 МБ/с. Таблица: `bench/results/2026-07-04-preliminary/smoke.md`. Попутно починены bench-скрипты (hardlink-корпус вместо junction — обходчики агентов пропускают reparse points; measure.ps1 для суб-секундных прогонов)
- [x] **Официальные серии замеров** (2026-07-04, Docker остановлен, ротация, медианы — [отчёт](bench/results/2026-07-04-official/README.md)):
  - bench-medium 5.4 ГБ тёплый: **C++ 3023 МБ/с > Go 2738 > Rust 2614** (разброс ≤3.1%)
  - стресс-файл 5.76 ГБ: RSS **C++ 2.35 ГБ** против Rust 15 ГБ / Go 16.8 ГБ (цельночитаемые файлы — риск OOM)
  - полный корпус 175 ГБ холодный: **Rust 1.69 ГБ/с ≈ C++ 1.65 >> Go 0.90**
  - счётчики событий всех участников совпали во всех сериях (121 485 348 на полном корпусе)
- [x] **Промежуточный decision record** → [docs/decision-record.md](docs/decision-record.md): после фикса памяти лидируют Go и C++ ноздря в ноздрю; финал после e2e и tail
- [x] **Фикс памяти Go/Rust** (2026-07-06, по жалобе владельца — Go съедал всю память): чанковый ридер + байтовый бюджет + стриминг головного файла. RSS на стрессе 5.76 ГБ: Go 16.8 ГБ → **52 МБ**, Rust 15 ГБ → **71 МБ**; golden 18 PASS сохранён, дифф-фаззинг стыков чанков чист. Перезамер: Go теперь №1 на medium (3077 МБ/с), Rust просел (2289 — сканер требует оптимизации)
- [x] **`--sink clickhouse` у всех трёх + e2e-серия** (2026-07-06): C++ 246 тыс. строк/с (RowBinary/WinHTTP свой) > Rust 226 (RowBinary/HTTP, std-only) > Go 176 (clickhouse-go v2); все 9 прогонов сверены по count(), sum(duration) совпала с оракулом до микросекунды. Отчёт: [2026-07-06-e2e](bench/results/2026-07-06-e2e/README.md)
- [x] **Бэкпорт байтового бюджета в core**: стресс-RSS 2368 → 72.7 МБ, golden/детерминизм сохранены
- [x] Decision record ред. 3: **фотофиниш C++ 89.9% vs Go 88.5%** (взвешенно по 85% измеренных весов), финал решает tail-серия (фаза 3)
- [x] **Follow-режим у всех трёх** (2026-07-08, единый контракт: полный шаринг, чекпоинты по ack с идентичностью volume+file index, idle-close 2 с только для `\n`-хвоста, backoff при недоступном CH) + **tail-харнесс** `tests/tail/` (генератор до 200 тыс. соб/с с удерживаемым файлом + 7 сценариев §4.5)
- [x] **Официальная tail-серия: все трое 7/7 PASS**, 0 потерь / 0 дублей везде вкл. kill -9; p95 латентности: Go 1595 < Rust 1686 < C++ 2049 мс. Серия поймала общий продовый баг — ленивые метаданные NTFS для удерживаемых писателем файлов (агенты слепли до закрытия файла) — исправлен у всех, правило «размер только через хэндл» зафиксировано. Отчёт: [2026-07-08-tail](bench/results/2026-07-08-tail/README.md)
- [x] **ФИНАЛЬНОЕ РЕШЕНИЕ** → [docs/decision-record.md](docs/decision-record.md) ред. 4: **агент — Go (90.2)**; C++ core (88.2) — эталонный нормализатор и библиотека; Rust (86.3) — консервируется как независимая реализация спеки. **Фаза 2 закрыта.**

## Фаза 3 — продуктизация агента-победителя (Go)

- [x] Follow-режим на живых логах — сделан в фазе 2 для всех участников (share-флаги, чекпоинты по ack, ротация, crash-recovery 0/0)
- [x] **Продуктовая схема** (2026-07-09): `--sink ...?schema=rich` → типизированная `tj.events`; маппинг = импортёр (паритет-гейт: нулевое расхождение по всем колонкам и гистограмме 63 ключей props); cityHash64 бит-в-бит с ClickHouse (агентские данные продолжают импортированные 121.5 млн строк). Найдены 2 бага импортёра (задокументированы)
- [x] **Вставка разогнана**: колоночные блоки ch-go + пул 3 коннектов → **510 тыс. строк/с (map) / 458 тыс. (rich)** — вдвое выше цели-паритета с C++ (246)
- [x] **Служба Windows + конфиг + метрики** (2026-07-09): YAML-конфиг (`tj-agent.example.yaml`, CLI перекрывает файл), `service install|start|stop|run` (graceful-дренаж по SCM-стопу; SCM-цикл не тестирован без прав администратора — код готов, шаги в deploy/agent/README.md), `/metrics` в формате Prometheus (promtool exit 0; lag/события/батчи/очередь; найден и исправлен баг сравнения часов «локальное-как-UTC»), scrape-job в deploy/prometheus (host.docker.internal проверен из контейнера), раздел в user-guide §5, алерт-правила в deploy/agent/README.md
- [x] KI-14: ошибки обхода при нуле файлов → exit 2 (юнит-тесты)
- [x] **SQL-нормализация** (2026-07-09, docs/sql-normalization.md): значения → `?` + позиционный массив `sql_params`, `param_count` как метрика; схлопывание на полном корпусе **6.28 млн уникальных → 14 403 (×436)**; перезаливка архива агентом (14 мин, 144 тыс. строк/с e2e, счётчик 121 485 348 совпал с нормализаторами — импортёр выведен из канона); дашборд СУБД на agg_query2 + панель «Деградация от числа параметров» (наглядно: 0.8 мс → 14.6 мс при росте IN с 2–10 до 101–1000). Найден и обойдён баг старого agg_query (терял до 35% счётчиков на слияниях); mem_limit CH поднят до 8g (bulk с 5 MV не влезал в 6g)
- [ ] Вывести старый agg_query/mv_agg_query из схемы (дашборды уже на agg_query2)
- [ ] SCM-цикл службы проверить из консоли администратора (install → start → stop → uninstall)
- [ ] Дашборд Grafana «Агент» поверх метрик Prometheus

### Фаза 3.6 — уроки Vector (ADR: docs/adr-vector.md, одобрено владельцем 2026-07-09)

- [ ] **Дисковый буфер (WAL) в follow-режиме** по референсу Vector: NDJSON-кадры с CRC32 в сегментах по 128 МиБ, fsync ≤500 мс, ack-точка = durable-запись в буфер (чекпоинт ТЖ двигается после буфера, не после CH), when_full=block, при I/O-ошибке буфера — жёсткая остановка, восстановление хвоста по чексуммам; метрики: байты/сегменты/возраст неотданного
- [ ] **ARC — адаптивная конкурентность вставки (AIMD)** вместо фиксированного пула 3: наращивать по хорошим RTT, резко сбрасывать по MEMORY_LIMIT/таймаутам (лекарство от инцидента bulk-перезаливки)
- [ ] **Карантин нераспознанного (dead letter)**: parse_skips не только считать — сырые блобы в карантин (файл/таблица) для разбора
- [ ] `tj-agent-go validate` (конфиг + каталоги + пинг БД, без запуска) и `tj-agent-go tap` (живая подсмотрка распарсенных событий без CH)
- [ ] Горячая перезагрузка конфига в режиме службы (watch); enrichment-таблицы (CSV: usr → отдел, ИБ → владелец)
- [ ] Маркировка гарантий доставки по компонентам в доке (at-least-once / best-effort)

## Фаза 4 — мульти-БД слой

- [ ] Интерфейс Store (InsertBatch / EnsureSchema / ApplyRetention / Health — storage-design §3) с **fanout-ack семантикой Vector**: при нескольких приёмниках событие подтверждено, только когда подтвердили ВСЕ
- [ ] Брокер как опциональная топология (решение владельца): `--sink kafka` (кандидаты Redpanda / NATS JetStream) + консюмер `ingest --from kafka` из того же бинаря
- [ ] Компонентная модель конфига (секции sources/transforms/sinks) — вместе с брокером/мульти-sink
- [ ] Бэкенд PostgreSQL (ниша: инсталляции до ~20–30 ГБ ТЖ/день)
- [ ] Бэкенд DuckDB («архив на ноутбуке», обмен через Parquet)
- [ ] Бэкенд MS SQL (columnstore)
- [ ] Кросс-бэкенд интеграционные тесты на одном мини-корпусе

## Фаза 5 — свой UI

- [ ] Сервер запросов (API поверх Store) — язык = победитель bake-off
- [ ] SPA: обзор → top-N → drill-down до сырого события; связка Context ↔ SQL; сравнение периодов
- [ ] Управление сбором: статусы агентов, покрытие logcfg (опционально — раздача logcfg.xml)

---

## Открытые вопросы

1. Таймзона: `timestamp` без TZ (локальное время сервера). При нескольких серверах в разных TZ агенту нужен `--tz`/автоопределение. Решить в фазе 3.
2. В корпусе нет DBPOSTGRS/SDBL — схема их поддерживает, но golden-кейсов нет. Нужен лог с PG-инсталляции.
3. Импортёр фазы 1: выбрасывать ли `Mem_86`-подобные пустые коллекции из обхода (209 файлов по 3 байта BOM) — да, фильтр по размеру уже есть (100 байт).
